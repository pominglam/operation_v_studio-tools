# Documentation index

The former single-file catalogue was split for navigation.

## Web UI (screen-by-screen)

Start here: **`docs/features/README.md`**

That index links to:

- **`docs/features/shared/`** — layout shell, navbar, roles, external-access behavior
- **`docs/features/screens/`** — **every Vue page**: Products, Purchase orders, Inventory & employee count, Pricing, Sync progress, TCG events, Maintenance

## Backend (routes, middleware, jobs, dense rules)

See **`docs/features/backend/system-catalog-services-and-http.md`**

Keep that file aligned with **`routes/api.php`**, **`routes/web.php`**, and **`app/Services/**`** when changing behavior.

## Formal requirements / specs

- **`docs/requirements/*.md`**
- **`docs/project-overview.md`**
